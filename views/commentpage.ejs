<% include ./partials/header %>
	<div class="ui  main text container segment">
		<div class="ui huge header">Comment on <%= blog.title %></div>
			<form class="ui form" action="/blogs/<%= blog.id %>/comments" method="POST">

				<div class="field">
					<label>Comment</label>
					<input type="text" name="comment[text]" placeholder="write comment..">
				</div>

				<div class="field">
					<label>Author</label>
					<input type="text" name="comment[author]" placeholder="author">
				</div>
					<input class="ui violet big basic button" type="submit">
			</form>
		</div>
	</div>
<% include ./partials/footer %>